# macOS
.DS_Store
**/.DS_Store

# Local / machine-specific / sensitive — created in place by bootstrap.sh,
# never committed (identity, secrets, per-machine env).
.localrc
.gitconfig.local
*.local
.env
.env.*
.netrc
secrets/

# Timestamped backups of replaced files
backups/

# Personal change log — kept locally for my own reference, not shipped to
# people copying the repo (git history is the record for them).
docs/STATE-CHANGES.md
