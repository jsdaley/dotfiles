# zsh/profile.home.zsh — home-machine-specific shell config.
# (Loaded only when ~/.config/dotfiles/profile == "home".)

# Nothing home-specific yet. Add personal aliases/env here, e.g.:
#   alias media='cd ~/Movies'
